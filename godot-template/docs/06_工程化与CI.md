# 工程化与 CI

## GitHub Actions

当前提供：

- `validate.yml`：JSON 校验、Godot 导入检查、PCK 打包。
- `release.yml`：手动触发的 release 构建示例。

## Godot CI 注意点

- CI 无图形环境时使用 `--headless`。
- 导出依赖 `export_presets.cfg`。
- 正式平台导出需要安装对应导出模板。
- Android/iOS/Steam 密钥放 GitHub Secrets。
